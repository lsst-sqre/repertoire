<!-- Delete the sections that don't apply -->

### Backwards-incompatible changes

- TAP schema configuration now requires `databaseUrl` instead of individual `database`, `databaseUser`, `databaseHost`, and `databasePort` fields.
